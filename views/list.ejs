<%- include('partials/header'); -%>
<div id="list">
    <% if (x && x.length) { %>
        <ul>
          <% x.forEach((item) => { %>
            <a href="download/<%=item%>" >
              
              <li><%= item %></li>
            </a>
          <% }) %>
        </ul>
      <% } %>
</div>
<%- include('partials/footer'); -%>
